# Microsoft Entra ID OAuth Configuration
# Copy this file to .env and fill in your actual values

# Your Microsoft Entra ID Application (client) ID
CLIENT_ID=your_client_id_here

# Your Microsoft Entra ID Application client secret
CLIENT_SECRET=your_client_secret_here

# Your Microsoft Entra ID Directory (tenant) ID
TENANT_ID=your_tenant_id_here

# OAuth Scopes (space-separated)
# Common scopes: openid profile email User.Read
SCOPE=openid profile email User.Read

# Redirect URI (must match what's configured in Azure App Registration)
REDIRECT_URI=http://localhost:8501/oauth2callback

# Microsoft Entra ID Base URL (URLs will be constructed automatically)
# For specific tenant (recommended): https://login.microsoftonline.com
# For multi-tenant apps: https://login.microsoftonline.com (will use /common endpoint)
BASE_URL=https://login.microsoftonline.com

# ProductsAgent API Configuration
# Base URL for the ProductsAgent API
PRODUCTS_API_BASE_URL=http://localhost:8000
